spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/carddemo
    username: carddemo
    password: carddemo
  jpa:
    hibernate:
      ddl-auto: validate

carddemo:
  seed:
    enabled: true
  security:
    csrf-cookie-secure: false
  cors:
    allowed-origins: http://localhost:5173

logging:
  level:
    com.carddemo.backend: DEBUG
